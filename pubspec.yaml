name: loan_app
description: "A new Flutter project."
publish_to: 'none'

# App version information
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # External Packages (Spacing is critical here!)
  cupertino_icons: ^1.0.2
  provider: ^6.1.1
  path_provider: ^2.1.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  # Design configuration
  uses-material-design: true

  # Assets section (Agar images use karni hon toh yahan add karein)
  # assets:
  #   - assets/images/
  
